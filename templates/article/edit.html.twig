{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Modifier l'article</h1>

        {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('article_index') }}" class="btn btn-secondary mt-3">Revenir aux articles</a>
        {{ include('article/_delete_form.html.twig') }}
    </div>
{% endblock %}